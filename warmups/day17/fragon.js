function getRondom(min,max) {
return math.floor(math.random()) *(max - min +1)+min;
}
var funtion(names) {
this names = names;
this.dragonName="";
this.generate =funntion(){
    var index = getRondom(0;name.length-1);
    this.dragonName = names[index];
    
}

var possibleNames = ["Ancient Dragon","Prowler","Mighty Grunt
"];
for(var i=1 ; i<100; i++){
var tempDragon.generate = new dragon(possibleNames);
tempDragon.generate();
                     console.log(tempDragon.dargonName);
                     
            
                     }                     